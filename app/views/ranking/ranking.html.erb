<h1><%= @title %></h1>

<ul>
  <% @items.each.with_index(1) do |item, index| %>
    <li>
      <%= index %>位：<%= link_to item.title, item %><br>
      <%= link_to (image_tag item.medium_image), item %>
    </li>
  <% end %>
</ul>

